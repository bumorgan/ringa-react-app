import { createMuiTheme } from '@material-ui/core/styles';

const ringa_theme = createMuiTheme({
    palette: {
        primary: {
            main: '#000000',
        },
        secondary: {
            main: '#f6b027',
        },
    },
  });

export default ringa_theme;